playbook	-> use of ping module

playbook01	-> test to create a file

playbook02	-> install packages with direct root acess

playbook03	-> install packages by escalating to sudo access only for
		   required tasks

playbook04	->
